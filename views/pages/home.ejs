<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head', {title}) %>
</head>
<body>
    <%- include('../partials/header') %>
    
    <section class="hero">
        <div class="container">
            <h1>Welcome to Our Community Portal</h1>
            <p>Join us for exciting events and connect with like-minded people!</p>
            <div class="mt-4">
                <a href="/events" class="btn btn-accent"><i class="fas fa-calendar-alt me-2"></i>Browse All Events</a>
                <a href="/contact" class="btn btn-outline-light ms-2"><i class="fas fa-envelope me-2"></i>Contact Us</a>
            </div>
        </div>
    </section>

    <div class="container main-container">
        <h2 class="section-heading">Upcoming Events</h2>
        
        <div class="row">
            <% upcomingEvents.forEach(event => { %>
                <div class="col-md-6">
                    <div class="card">
                        <img src="/images/<%= event.image %>" class="card-img-top" alt="<%= event.title %>">
                        <div class="card-body">
                            <span class="date-badge"><i class="far fa-calendar-alt me-1"></i><%= event.date %></span>
                            <span class="location-badge"><i class="fas fa-map-marker-alt me-1"></i><%= event.location %></span>
                            <h5 class="card-title"><%= event.title %></h5>
                            <a href="/events" class="btn btn-outline-primary mt-2">Learn More</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
        
        <div class="text-center mt-5">
            <a href="/events" class="btn btn-primary">View All Events</a>
        </div>
        
        <section class="mt-5 pt-5">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h2 class="section-heading">Community Highlights</h2>
                    <p>Our community portal serves as a hub for local events, bringing people together and fostering community spirit.</p>
                    <p>From charity events to art exhibitions, we feature a diverse range of activities to cater to different interests.</p>
                    <a href="/about" class="btn btn-outline-primary mt-3">Learn About Our Team</a>
                </div>
                <div class="col-md-6">
                    <div class="ratio ratio-16x9">
                        <img src="/api/placeholder/600/400" alt="Community Highlights" class="rounded shadow">
                    </div>
                </div>
            </div>
        </section>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>